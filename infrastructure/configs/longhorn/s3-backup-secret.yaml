apiVersion: v1
kind: Secret
metadata:
  name: s3-backup-credentials
  namespace: longhorn-system
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: ${SECRET_S3_ACCESS_ID}
  AWS_SECRET_ACCESS_KEY: ${SECRET_S3_ACCESS_KEY}
  AWS_ENDPOINTS: ${SECRET_S3_ENDPOINT}
