apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pgo-release
  namespace: kube-system
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: pgo
    namespace: flux-system
  values:
    debug: false
    singleNamespace: false
