apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
  namespace: media
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`radarr.${SECRET_DOMAIN}`)
      kind: Rule
      services:
        - name: radarr
          port: 80
      middlewares:
        - name: trusted-only
          namespace: kube-system
  tls: {}
