apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: seerr
  namespace: media
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`seerr.${SECRET_DOMAIN}`)
      kind: Rule
      services:
        - name: seerr-seerr-chart
          port: 80
      middlewares:
        - name: trusted-only
          namespace: kube-system
  tls: {}
